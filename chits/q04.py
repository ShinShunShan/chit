"""Q4: Toy RSA timing experiment (standalone, no external libs).

This mirrors q02 but kept as a separate file for the exercise. Uses the
same toy RSA implementation (probable prime generation + textbook RSA).
"""

from q02 import run


if __name__ == '__main__':
    run()
